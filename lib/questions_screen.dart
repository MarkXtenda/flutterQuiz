import 'package:flutter/material.dart';

class QuestionsScreen extends StatelessElement {
  // const QuestionsScreen({super.key});

  @override
  Widget build(context) {
    return const Text("Questions");
  } 
}